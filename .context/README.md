# .context/

Gitignored AI context for the logdive project. Written from a full cold-start
exploration on 2026-06-01, reflecting the project state at v0.2.1.

Do not commit this directory. Do not include it in pull requests. Update the
relevant files after each milestone ships — stale context is worse than no
context because it misleads the next session into reasoning from wrong
premises.

Files in this directory are written from what actually exists in the code, not
from what was planned or assumed. If any file contradicts the current code, the
code wins; update or remove the offending section.
